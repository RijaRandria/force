{% extends "maitre.twig" %}
{% block content %}
<div class="container text-center">
  <h3 ><i class="fa fa-book"></i>&nbsp;Liste des types de permis&nbsp;
    <a class="btn btn-outline-success btn-sm" href="{{path_for('newpermis')}}"><i class="fa fa-copy"></i>&nbsp;Nouveau type de permis</a>
  </h3>

<table class="table">
  <thead class="thead-light">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Type de permis</th>
    </tr>
  </thead>
  <tbody>
    {% for ligne in lignes %}
      <tr>
        <td> {{ligne.id}}   </td>
        <td> {{ligne.permis}}</td>
      </tr>
    {% endfor %}

  </tbody>
</table>
{% endblock %}
{# Exemple RouterJS #}
{% block javascript %}
  {{ parent() }}
{% endblock %}
