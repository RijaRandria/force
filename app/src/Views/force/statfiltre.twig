{% extends "maitre.twig" %}
{% block title %}{{titre}}{% endblock %}
{% block content %}
<style type="text/css">
      .fond input[type="radio"] {
        display: none;
      }
      .fond input[type="checkbox"] {
        display: none;
      }
     

      .toolbar label {
        display: inline-block;
        background-color: silver;
        padding: 4px 11px;
        font-family: Arial;
        font-size: 16px;
        cursor: pointer;
        margin : auto
      }
       .toolbar1 label {
        display: inline-block;
        background-color: silver;
        padding: 4px 11px;
        font-family: Arial;
        font-size: 16px;
        cursor: pointer;
        margin : auto
      }

      .toolbar input[type="radio"]:checked+label {
        background-color: green;
        color : white;
      }
       .toolbar input[type="checkbox"]:checked+label {
        background-color: green;
        color : white;
      }
      
      .toolbar1 input[type="radio"]:checked+label {
        background-color: red;
        color : white;
      }
    
   
</style>

<div id="login-page">


    <h2 class="text-center">{{titre}}</h2>

     <form class="form-login" id="auth" role="form" method="POST" action="{{ path_for('statistique') }}">
        <h2 class="form-login-heading">Liste des Chantiers</h2>
        </br>
        <div class="form-group text-center" >
        
            {% for lieu in lieux %}
             <div class="col-md-12">
                <div class =" fond toolbar label " >
                        <input type = "radio" name = "idlieu" id ='{{lieu.lieu}}' value ='{{lieu.idlieu}}'></input>
                        <label class =" d-grid gap-2 col-6 mx-auto btn btn-light" for = '{{lieu.lieu}}'> {{lieu.lieu}}</label>
                </div>
             </div>   
            {% endfor %}
            </br>
            Du <input type = "date" name ="datedeb"> Au <input type = "date" name ="datefin">
         
        </div>  

         <div class="form-check text-center">
              <button class="btn btn btn-primary" type="submit"><i class="fa fa-ok"></i> VALIDER</button>
              <a href="{{ path_for('home') }}" class="btn btn-secondary">Annuler</a>
        </div>
    </form>
   

</div>

{% endblock %}
{% block javascript %}
  {{ parent() }}

  <script>

         function accepter () {

                 if ((document.getElementById('nom').value != "") &&
                    (document.getElementById('prenom').value != ""))
                 {


                     var accueil2 =  "Validation  ";
                     //var accueil = (accueil1);
                     alert(accueil2);
                     return true;

                 }


                 else {


                     alert ("Veuillez saisir nom et prénom");
                     return false

                 }

         }


  </script>

{% endblock %}
