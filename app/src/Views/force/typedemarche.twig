{% extends "maitre.twig" %}
{% block content %}
<div class="container text-center">
  <h3 ><i class="fa fa-book"></i>&nbsp;Liste des types de Démarche&nbsp;
    <a class="btn btn-outline-success btn-sm" href="{{path_for('newdetaildemarche')}}"><i class="fa fa-copy"></i>&nbsp;Nouveau type de démarcche</a>
  </h3>

<table class="table">
  <thead class="thead-light">
    <tr>
      <th scope="col">Type de démarche</th>
      <th scope="col"></th>
      <th scope="col">Détails de démarche</th>
    </tr>
  </thead>
  <tbody>
    {% for ligne in lignes %}
      <tr>
        <td> {{ligne.demarche}}   </td>
        <td> ----------------------------------></td>
        <td> {{ligne.typedemarche}}</td>
      </tr>
    {% endfor %}

  </tbody>
</table>
{% endblock %}
{# Exemple RouterJS #}
{% block javascript %}
  {{ parent() }}
{% endblock %}
